export enum SizeEnum {
  small = 'small',
  medium = 'medium',
  large = 'large',
}

export enum TypeEnum {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
  action = 'action',
  danger = 'danger',
}
